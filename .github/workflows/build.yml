name: 🚀 Build Project

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    # 👉 один раз задаём рабочую папку для всех шагов
    defaults:
      run:
        working-directory: ./

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # cache: 'npm'  # ВАЖНО: кеш выключен, чтобы не требовал lock-файл на этом шаге
          # cache: 'npm'                <-- удалить
          # cache-dependency-path: ...  <-- удалить

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔨 Build project
        run: npm run build

      - name: 📤 Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

