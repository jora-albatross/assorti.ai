# Руководство по ручному деплою в Cloudflare Pages

Красота! По скринам вижу, ты почти всё сделал правильно.  
Вот как зафиксировать “ручной деплой” и не позволять Cloudflare Pages запускать билды при каждом коммите:

---

## 1. Branch Control (контроль веток)
- Production branch: `main`
- **Отключи галочку** `Enable automatic production branch deployments`  
  → теперь Pages не будет автоматически деплоить сайт при каждом пуше в `main`.

Preview branch:  
- Если хочешь тестировать на отдельных ветках — укажи их (например, `dev`, `feature/*`).  
- Если не нужно — выбери `None`.

---

## 2. Build watch paths (пути, отслеживаемые для билдов)
- Очисти поле `Include paths` (оставь пустым).  
- Очисти поле `Exclude paths`.  
Теперь Pages не будет запускать билд при каждом изменении файла.

---

## 3. Как деплоить вручную
- Перейди на вкладку **Deployments** → **Trigger deploy**.  
- Выбери `Deploy from branch` → `main` (или другую нужную).  

Это и будет твой **ручной деплой**.

---

## 4. Что мы получили
✅ Контроль остаётся у нас.  
✅ GitHub сохраняет историю, можно учиться и коммитить спокойно.  
✅ Cloudflare Pages не триггерит авто-билды.  
✅ Мы сами решаем, когда запускать новый билд.

---

## Совет
Хочешь — добавим это руководство прямо в `/docs/deploy-manual.md` внутри проекта, чтобы всегда было под рукой.

## Как вернуть автодеплой (если пригодится позже)
В Branch control снова включи галочку Enable automatic production branch deployments и выбери нужные Preview branches.
При необходимости в Build watch paths укажи точечные маски, чтобы билд триггерился только на полезные изменения, например:

Include paths:
src/**
public/**
package.json
astro.config.mjs
tailwind.config.cjs

Exclude paths:
projects/**
docs/**
.github/**

## Если вдруг старый фронт “прилипает”
Иногда Pages/браузер кэшируют статику. Для обновления:
Жёсткое обновление в браузере (Cmd/Ctrl + Shift + R).
В Cloudflare Caching → Purge → Purge Everything (обычно не требуется, но помогает, если менялась OG-картинка/иконки).

# Руководство по ручному деплою в Cloudflare Pages

Красота! По скринам вижу, ты почти всё сделал правильно.  
Вот как зафиксировать “ручной деплой” и не позволять Cloudflare Pages запускать билды при каждом коммите:

---

## 1. Branch Control (контроль веток)
- Production branch: `main`
- **Отключи галочку** `Enable automatic production branch deployments`  
  → теперь Pages не будет автоматически деплоить сайт при каждом пуше в `main`.

Preview branch:  
- Если хочешь тестировать на отдельных ветках — укажи их (например, `dev`, `feature/*`).  
- Если не нужно — выбери `None`.

---

## 2. Build watch paths (пути, отслеживаемые для билдов)
- Очисти поле `Include paths` (оставь пустым).  
- Очисти поле `Exclude paths`.  
Теперь Pages не будет запускать билд при каждом изменении файла.

---

## 3. Как деплоить вручную
- Перейди на вкладку **Deployments** → **Trigger deploy**.  
- Выбери `Deploy from branch` → `main` (или другую нужную).  

Это и будет твой **ручной деплой**.

---

## 4. Что мы получили
✅ Контроль остаётся у нас.  
✅ GitHub сохраняет историю, можно учиться и коммитить спокойно.  
✅ Cloudflare Pages не триггерит авто-билды.  
✅ Мы сами решаем, когда запускать новый билд.

---

## Совет
Хочешь — добавим это руководство прямо в `/docs/deploy-manual.md` внутри проекта, чтобы всегда было под рукой.

---------------------------------------------------------

Когда я предложил workflow “деплой по тэгу релиза”, это означает:
мы можем прописать в .github/workflows/deploy.yml правила, чтобы Cloudflare (или любая CI/CD система) запускала деплой только тогда, когда в репозитории создаётся Git-тег вроде v1.0.0.

## Как это работает шаг за шагом:
🔧 В deploy.yml указываем on: push: tags: - "v*.*.*".
Это значит: деплой запустится только если пушим тэг, например:

git tag v1.0.0
git push origin v1.0.0

⚡ GitHub Actions видит, что пуш был на новый тэг → запускает билд.
🚀 После успешного билда workflow вызывает Cloudflare Pages API (через wrangler или curl с твоим API-токеном), и происходит деплой.
🎯 Итог: обычные коммиты (git push) не триггерят деплой. Всё остаётся в GitHub.
Деплой только тогда, когда ты явно создаёшь “релиз” через тэг.

## Зачем это нужно?
🔒 Контроль: ты решаешь, какая версия кода пойдёт на сайт.
📦 Чистота: каждое обновление на проде будет привязано к конкретному релизу (v1.2.3).
📚 Удобно для документации и истории: всегда знаешь, какая версия задеплоена.

## 1. GitHub Actions как “оркестр”
Мы добавляем файл .github/workflows/deploy.yml.
В нём пишем: деплой запускается только при пуше тэга v*.*.*.
Это значит, что когда ты работаешь с кодом → можешь коммитить хоть 100 раз в день.
Но пока не создашь релиз (например, git tag v1.0.0 && git push origin v1.0.0) — сайт не деплоится.

## 2. Cloudflare как “исполнитель”
GitHub Action берёт свежий код, билдит, и через API-токен шлёт команду Cloudflare Pages.
Cloudflare заливает билд на продакшен.

## 3. Что это даёт?
🔒 Контроль: только осознанные релизы попадают в прод.
📦 Версионирование: ты всегда знаешь, какая версия кода работает (v1.0.1, v1.2.0 и т.д.).
⚡ Скорость: работаешь над фичами без риска “сломать сайт” случайным пушем.
📚 История: можно откатиться на прошлый релиз за 2 минуты.

## 4. Будущее масштабирование
При росте команды:
– разработчики коммитят в фиче-ветки,
– тестер/PM проверяет,
– только потом создаётся релиз-тэг.

Всё автоматически фиксируется и деплоится прозрачно.

## ▶️ Как запустить деплой по тэгу
Локально в репозитории:
# 1) Сделай коммиты и запушь изменения в main (или нужную ветку)
git add .
git commit -m "chore: prepare v0.1.0"
git push

# 2) Создай тег релиза и запушь его
git tag v0.1.0
git push origin v0.1.0

После пуша тэга GitHub Actions автоматически запустит workflow и задеплоит dist/ в Cloudflare Pages.
(Также можно запустить вручную в Actions → Deploy to Cloudflare Pages (by tag) → Run workflow.)

## ℹ️ Что такое .yml / YAML?
.yml — это расширение файла, где используется формат YAML (YAML Ain’t Markup Language).
YAML — человеко-читаемый язык разметки для описания конфигураций/настроек.

Применяется в:
GitHub Actions (workflows в .github/workflows/*.yml)
Docker Compose (docker-compose.yml)
Kubernetes (манифесты)
многие CI/CD, конфиги сервисов и т.д.

Почему YAML? Он простой: вложенность задаётся отступами, ключ-значение пишется как key: value. Это удобно для конфигов, которые часто читают и правят люди.

env:
  NODE_ENV: production
features:
  - preview
  - analytics

## Workflow на превью-деплой для PR (каждый Pull Request будет получать свой preview URL, но на production ничего не попадёт, пока не смержишь)
## 1) Workflow для превью на PR
Создай файл: .github/workflows/preview.yml

Примечание: cloudflare/pages-action@v1 публикует превью, если branch ≠ production branch. 
            Мы передаём github.head_ref, так что это будет preview deployment. 
            URL превью появится в логах шага деплоя и в разделе Deployments в Cloudflare Pages.

## 2) Что уже есть (и важно не ломать)
У тебя есть deploy.yml “по тэгу” → он выкатывает production.
В Cloudflare Pages мы отключили автодеплой с веток — чтобы не было двойных билдов.

Секреты:
CF_API_TOKEN (Pages: Edit + Account Settings Read)
CF_ACCOUNT_ID
CF_PAGES_PROJECT

Они же используются и в превью-воркфлоу.

## 3) Как это теперь работает
📦 Обычные коммиты в feature-ветку → создаёшь PR → Actions собирает сайт и деплоит preview (отдельный URL, не main).
🧪 Проверяешь превью (дизайн, контент, верстку).
✅ Готово? Мёржишь PR → ничего ещё не деплоится на прод.
🏷️ Хочешь выпуск на прод → ставишь тег vX.Y.Z → срабатывает deploy.yml → деплой на production.

## 4) Рекомендации по контролю
Branch protection для main: требовать успешный статус CI перед merge.
(Опционально) Разрешить merge только через PR (без прямого пуша в main).
(Опционально) Добавить линт/тест шаги перед сборкой (чтобы ловить ошибки до деплоя).

## 5) Краткая памятка по YAML
Файл с расширением .yml / .yaml — конфигурация в формате YAML (читаемый человеком, отступы = вложенность).
В GitHub Actions YAML-файлы лежат в .github/workflows/ и описывают:
on: когда запускать (push, pull_request, tags, вручную),
jobs / steps: что делать (установить Node, собрать, задеплоить),
секреты и переменные, окружения, кэширование и т.д.


