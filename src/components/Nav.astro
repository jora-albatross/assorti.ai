---
const links = [
  { href: "/", label: "Главная" },
  { href: "/recipes/", label: "Рецепты" },
  { href: "https://t.me/aiprofithub", label: "AI Profit Hub" },
];
---

<nav class="w-full">
  <div class="mx-auto max-w-6xl px-4 py-4 flex items-center justify-between">
    <a href="/" class="flex items-center gap-2">
      <svg width="26" height="26" viewBox="0 0 24 24" aria-hidden="true" class="shrink-0">
        <defs>
          <linearGradient id="ag" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0" stop-color="#7c3aed" />
            <stop offset="1" stop-color="#06b6d4" />
          </linearGradient>
        </defs>
        <rect x="2" y="2" width="20" height="20" rx="6" fill="url(#ag)"/>
      </svg>
      <span class="text-lg font-bold">AssortiAI</span>
    </a>

    <ul class="hidden sm:flex items-center gap-5">
      {links.map(l => (
        <li><a class="text-sm text-zinc-600 hover:text-black transition" href={l.href}>{l.label}</a></li>
      ))}
    </ul>

    <a href="/#get-started" class="hidden sm:inline-block text-sm px-3 py-2 rounded-lg bg-black text-white hover:opacity-90 transition">
      Начать
    </a>
  </div>
</nav>
